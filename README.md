JS Beautify Safari Extension
============================

Adds a context menu to Safari 5. Right-click on any source code page with JS or JSON and this extension will automatically make that code prettier.